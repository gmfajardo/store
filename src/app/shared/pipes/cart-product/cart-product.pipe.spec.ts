import { CartProductPipe } from './cart-product.pipe';

describe('CartProductPipe', () => {
  it('create an instance', () => {
    const pipe = new CartProductPipe();
    expect(pipe).toBeTruthy();
  });
});
